import React from "react";
import Project from "../components/Project";

const Design = ({ allProjects, icon }) => {
  return <Project allProjects={allProjects} icon={icon} />;
};

export default Design;
